# Chiave API Anthropic (Claude) — richiesta per il parsing PDF con visione IA
ANTHROPIC_API_KEY=sk-ant-...

# Usato solo in sviluppo locale (il frontend Vite proxya le chiamate API)
# VITE_API_URL=http://127.0.0.1:8001
